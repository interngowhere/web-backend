// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/interngowhere/web-backend/ent/thread"
	"github.com/interngowhere/web-backend/ent/threadkudo"
	"github.com/interngowhere/web-backend/ent/user"
)

// ThreadKudo is the model entity for the ThreadKudo schema.
type ThreadKudo struct {
	config `json:"-"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// ThreadID holds the value of the "thread_id" field.
	ThreadID int `json:"thread_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ThreadKudoQuery when eager-loading is set.
	Edges        ThreadKudoEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ThreadKudoEdges holds the relations/edges for other nodes in the graph.
type ThreadKudoEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Thread holds the value of the thread edge.
	Thread *Thread `json:"thread,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ThreadKudoEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ThreadOrErr returns the Thread value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ThreadKudoEdges) ThreadOrErr() (*Thread, error) {
	if e.loadedTypes[1] {
		if e.Thread == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: thread.Label}
		}
		return e.Thread, nil
	}
	return nil, &NotLoadedError{edge: "thread"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ThreadKudo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case threadkudo.FieldThreadID:
			values[i] = new(sql.NullInt64)
		case threadkudo.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ThreadKudo fields.
func (tk *ThreadKudo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case threadkudo.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				tk.UserID = *value
			}
		case threadkudo.FieldThreadID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field thread_id", values[i])
			} else if value.Valid {
				tk.ThreadID = int(value.Int64)
			}
		default:
			tk.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ThreadKudo.
// This includes values selected through modifiers, order, etc.
func (tk *ThreadKudo) Value(name string) (ent.Value, error) {
	return tk.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the ThreadKudo entity.
func (tk *ThreadKudo) QueryUser() *UserQuery {
	return NewThreadKudoClient(tk.config).QueryUser(tk)
}

// QueryThread queries the "thread" edge of the ThreadKudo entity.
func (tk *ThreadKudo) QueryThread() *ThreadQuery {
	return NewThreadKudoClient(tk.config).QueryThread(tk)
}

// Update returns a builder for updating this ThreadKudo.
// Note that you need to call ThreadKudo.Unwrap() before calling this method if this ThreadKudo
// was returned from a transaction, and the transaction was committed or rolled back.
func (tk *ThreadKudo) Update() *ThreadKudoUpdateOne {
	return NewThreadKudoClient(tk.config).UpdateOne(tk)
}

// Unwrap unwraps the ThreadKudo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tk *ThreadKudo) Unwrap() *ThreadKudo {
	_tx, ok := tk.config.driver.(*txDriver)
	if !ok {
		panic("ent: ThreadKudo is not a transactional entity")
	}
	tk.config.driver = _tx.drv
	return tk
}

// String implements the fmt.Stringer.
func (tk *ThreadKudo) String() string {
	var builder strings.Builder
	builder.WriteString("ThreadKudo(")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", tk.UserID))
	builder.WriteString(", ")
	builder.WriteString("thread_id=")
	builder.WriteString(fmt.Sprintf("%v", tk.ThreadID))
	builder.WriteByte(')')
	return builder.String()
}

// ThreadKudos is a parsable slice of ThreadKudo.
type ThreadKudos []*ThreadKudo
